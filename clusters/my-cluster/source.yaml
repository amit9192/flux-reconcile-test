apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-reconcile-test
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/REPLACE_ME/flux-reconcile-test
  ref:
    branch: main

